# ── Frontend Production Environment ──
# Place this file in frontend/.env.production before running: npm run build

# API URL — points to your domain's backend
# If backend is on same domain: /api
# If backend is on subdomain: https://api.madhanarts.in/api
VITE_API_URL=/api
